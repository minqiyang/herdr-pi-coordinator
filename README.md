# Herdr + Pi coordinator

Declared version: **6.2-draft**. Identity is filename + declared version, not this folder name.

| File | Owns |
|---|---|
| `coordinator_standard.md` | protocol, authority, state, gates, recovery |
| `herdr_pi_runtime.md` | Herdr/Pi process, lock/CAS, GitHub publication |
| `routing_table.json` | routes, models, triggers |

A project adopts these files with `PROJECT_INITIALIZED` or `PROJECT_RECONFIGURED`. Do not restate routing in prompts; point at this repo and the declared version.
